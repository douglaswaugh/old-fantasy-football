using AlgorithmFinder.Application;
using NUnit.Framework;

namespace AlgorithmFinder.Tests
{
    [TestFixture]
    public class PointsPredictorTests
    {
        [Test]
        public void FirstTest()
        {
            // needs results/fixtures/predictions
            // looks at predicted result
            // calculates points ratios
            // calculates points based on points ratios
        }
    }
}